import { EditGate } from "@launchblitz/ui";

export function IdeaCapture() {
  return (
    <EditGate
      summary="Capture raw text or transcribed voice and normalize it into a build seed."
      title="Stage 1 · Idea Capture"
    />
  );
}
